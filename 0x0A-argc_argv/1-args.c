#include "main.h"
#include <stdio.h>

/**
 * main - prints the number of args.
 * @argc: argument count.
 * @argv: argument vector.
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	(void) argv;
	return (0);
}
